StatusStats = require '../lib/status-stats'
{$} = require "atom-space-pen-views"

describe "StatusStats", ->
  it "triggers the toggle command when activated", ->
    # I would love to know how to write this test.

  it "stops triggering the toggle command when deactivated", ->
    # I would love to know how to write this test.

  it "shows the stats on the left side of the status bar", ->
    # I would love to know how to write this test.
